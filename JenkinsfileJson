node {
    def mvnHome
    stage('Preparation') { // for display purposes
        sh "cat s32.json > tempfile"
        sh "cat tempfile"
    }
    stage('Build') {
        
    }
    stage('Results') {
        
    }
}
